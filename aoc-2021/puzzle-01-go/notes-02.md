# Notes part 2

```go
// first create a new slice of sums
var windowVal int
var windowCounter int
if windowCounter < 2 {
    // add val to window val?
    windowCounter++
}
// then compare all the sums same way as before
```
